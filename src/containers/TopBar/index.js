import React from "react";
import { Layout } from "antd";

const { Header } = Layout;
function TopBar() {
  return (
    <Header className="admin-app-wrapper-header">
      <h1>Hello</h1>
    </Header>
  );
}

export default TopBar;
